import SwiftUI

struct SummaryView: View {
   
   let madlib : Madlib
    
    
    
    var body: some View {
        VStack {
            Text("The \(madlib.noun) \(madlib.verb) \(madlib.adjective)")
                }
            }
        }
    

